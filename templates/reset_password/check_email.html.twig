{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="w-full max-w-md p-8 space-y-6">
            <h1 class="text-2xl font-bold text-center">Email envoyé</h1>

            <div class="space-y-4 text-gray-600">
                <p>
                    Si un compte correspond à votre email, un lien de réinitialisation vient de vous être envoyé.
                    Ce lien expirera
                    dans 1 heure.
                </p>

                <p>
                    Si vous ne recevez pas d'email, vérifiez votre dossier spam ou
                    <a href="{{ path('app_forgot_password_request') }}" class="text-green-500 underline">réessayez</a>.
                </p>
            </div>

            <p class="text-sm text-center text-gray-600">
                <a href="{{ path('app_login') }}" class="text-green-500 underline">Retour à la connexion</a>
            </p>
        </div>
    </div>
{% endblock %}
